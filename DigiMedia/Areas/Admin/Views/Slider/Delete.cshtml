@using DigiMedia.Core.Models
@model Slider

<div class="container">
    <div class="row">
        <div class="col-6">
            <form method="post" asp-action="DeletePost">
                <div class="card" style="width: 18rem;">
                    <img src="~/uploads/sliders/@Model.ImageUrl" class="card-img-top" alt="...">
                    <div class="card-body">
                        <h5 class="card-title">@Model.Title</h5>
                        <p class="card-text">@Model.Profession</p>
                        <input value="@Model.Id" hidden asp-for="@Model.Id" />
                        <a asp-action="Index" class="btn btn-warning">Go Back</a>
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>